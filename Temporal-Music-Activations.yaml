name: temporal-music-activations-meta
channels:
  - conda-forge

dependencies:
  - python=3.12

# This file is a lightweight pointer to platform-specific env files.
#
# Apple Silicon / macOS:
#   conda env create -f Temporal-Music-Activations-apple.yaml
#   conda activate temporal-music-activations-apple
#
# NVIDIA CUDA (12.1):
#   conda env create -f Temporal-Music-Activations-cuda.yaml
#   conda activate temporal-music-activations-cuda
